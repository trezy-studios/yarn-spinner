/**
 * @typedef {object} CustomLogger
 * @property {Function} [error] Log error to the console.
 * @property {Function} [group] Start grouping logs.
 * @property {Function} [groupEnd] Stop grouping logs.
 * @property {Function} [info] Log to the console.
 * @property {Function} log Log to the console.
 * @property {Function} [warn] Log warning to the console.
 */
export const CustomLogger = {}
