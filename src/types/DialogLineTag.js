/**
 * @typedef {object} DialogLineTag
 * @property {string} key The key of the tag.
 * @property {*} value The value of the tag
 * @property {string} original The original, unparsed tag.
 */
export const DialogLineTag = {}
