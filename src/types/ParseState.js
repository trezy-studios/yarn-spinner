/**
 * @typedef {object} ParseState
 * @property {import('bbcode-ast').Parser} bbcodeParser The BBCode parser to use for this script.
 * @property {object} context The machine's context.
 * @property {string[]} validMarkup A list of valid markup tags.
 */
