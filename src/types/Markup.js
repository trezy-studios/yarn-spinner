/**
 * @typedef {object} Markup
 * @property {number} index The start index of the markup's contents.
 * @property {number} length The length of the markup's contents.
 * @property {string} name The name of the markup.
 * @property {Map<string, import('./Value.js').Value>} parameters A mapping
 */
export const Markup = {}
